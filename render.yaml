services:
  - type: web
    name: controlpro-advisor-os-v10-workshop-lock
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
